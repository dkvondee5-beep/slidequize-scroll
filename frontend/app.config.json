// app.config.js - Expo Configuration File
// This file is REQUIRED for Expo to bundle your app and load environment variables.

// Load environment variables from .env file
require('dotenv').config();

module.exports = {
  expo: {
    name: 'SlideQuiz Scroll',
    slug: 'slidequiz-scroll',
    version: '1.0.0',
    orientation: 'portrait',
    extra: {
      // This makes your Clerk key available in the app via Constants.expoConfig.extra
      clerkPublishableKey: process.env.EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY,
    },
  },
};
